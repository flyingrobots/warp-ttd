#!/usr/bin/env bash
set -euo pipefail

if git diff --cached --name-only --diff-filter=ACM | grep -q '^docs/topics/'; then
  npm run docs:check
  npm run docs:evidence
  npm run docs:impact
  npm run docs:eval
fi
