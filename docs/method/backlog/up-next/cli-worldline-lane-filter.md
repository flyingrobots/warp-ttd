# CLI worldline --lane filter

The `worldline --json` command outputs all ticks across all lanes.
An agent should be able to filter by lane:

    worldline --json --lane wl:alpha

Surfaced during cycle 0010 agent playback.
